# A178｜板凳龙行进路径与速度控制优化

> [!important] 解读口径
> 本文解读建立在 807 页全量 OCR 中该论文的全部页面、确定性 Evidence IR、摘要页与模型评价页上。普通段落已人工校订；公式、上下标、表格数字和单位仍须按页码回看源 PDF。

## 一句话定位

采用微元法、同心圆近似和粗到细搜索，追求较低计算成本；其 Q3 简化较强，结果可作为对照而非直接作为标准答案。

## 逐问路线总览

| 子问题 | 任务 | 核心方法 | 主要结果 | 原文页 |
|---|---|---|---|---|
| Q1 | 求板凳龙盘入状态。 | 极坐标螺线、微元法建立龙头微分方程，利用板凳不可伸长和把手位于螺线的约束递推位置与速度。 | 输出 0–300 s 的逐秒状态。 | p.1、p.3 |
| Q2 | 求首次碰撞时刻。 | 用同心圆近似把碰撞时间预估到约 410 s，再在 [400,420] s 内用叉乘法和变步长搜索精化。 | 碰撞时刻约 412.4739 s。 | p.1、p.3 |
| Q3 | 求最小螺距。 | 只考虑龙头与第一节龙身，并只检查进入调头空间前的局部时间段，用二分法搜索。 | 得到 0.4000 m；由于简化范围较强，明显小于多数同题论文约 0.45034 m。 | p.1、p.3 |
| Q4 | 建立四阶段调头运动方程。 | 将路径分为盘入、两段圆弧、盘出，用单值函数规避极角多值约束，并证明调头曲线长度不变。 | 得到关键时刻的全龙位置和速度。 | p.1 |
| Q5 | 最大化龙头速度。 | 分析曲率半径、板凳方向夹角与速度峰值的关系，先锁定小圆弧末端附近，再用二分法精搜。 | 龙头最大速度约 1.2462 m/s，声称误差控制在 0.0001 m/s。 | p.1、p.3 |

## 模型与算法分工

### 模型（描述数学关系）

- 螺线微分运动模型
- 同心圆碰撞近似模型
- 叉乘几何碰撞模型
- 四阶段分段路径模型
- 曲率—速度峰值模型

### 算法（负责求解或估计）

- 微元法
- 变步长搜索
- 二分法
- 离散仿真

## 分问题精读

### Q1｜求板凳龙盘入状态。

**怎么建模：** 极坐标螺线、微元法建立龙头微分方程，利用板凳不可伸长和把手位于螺线的约束递推位置与速度。

**得到什么：** 输出 0–300 s 的逐秒状态。

**来源锚点：** p.1、p.3；更细的原文证据见 `source_map.json` 与 `workflow/ir/paper_ir.json`。

### Q2｜求首次碰撞时刻。

**怎么建模：** 用同心圆近似把碰撞时间预估到约 410 s，再在 [400,420] s 内用叉乘法和变步长搜索精化。

**得到什么：** 碰撞时刻约 412.4739 s。

**来源锚点：** p.1、p.3；更细的原文证据见 `source_map.json` 与 `workflow/ir/paper_ir.json`。

### Q3｜求最小螺距。

**怎么建模：** 只考虑龙头与第一节龙身，并只检查进入调头空间前的局部时间段，用二分法搜索。

**得到什么：** 得到 0.4000 m；由于简化范围较强，明显小于多数同题论文约 0.45034 m。

**来源锚点：** p.1、p.3；更细的原文证据见 `source_map.json` 与 `workflow/ir/paper_ir.json`。

### Q4｜建立四阶段调头运动方程。

**怎么建模：** 将路径分为盘入、两段圆弧、盘出，用单值函数规避极角多值约束，并证明调头曲线长度不变。

**得到什么：** 得到关键时刻的全龙位置和速度。

**来源锚点：** p.1；更细的原文证据见 `source_map.json` 与 `workflow/ir/paper_ir.json`。

### Q5｜最大化龙头速度。

**怎么建模：** 分析曲率半径、板凳方向夹角与速度峰值的关系，先锁定小圆弧末端附近，再用二分法精搜。

**得到什么：** 龙头最大速度约 1.2462 m/s，声称误差控制在 0.0001 m/s。

**来源锚点：** p.1、p.3；更细的原文证据见 `source_map.json` 与 `workflow/ir/paper_ir.json`。

## 值得学习的地方

- 先用同心圆近似缩小碰撞搜索区间，效率高且最终碰撞时刻与其他论文一致。
- 微分方程解析关系可减少逐步积分误差。
- 对速度峰值位置给出物理解释。

## 局限与复核重点

- Q3 只检查龙头与第一节且只看局部时间窗，导致 0.4000 m 与更完整碰撞模型明显不一致。
- 连续碰撞被离散时刻检测，可能漏掉步长内事件。
- 现实随机扰动和板凳形变未纳入。

## 可迁移经验

- 可先构造便宜的近似模型定位搜索区间，再调用精确判定。
- 若简化改变了可行域，必须用完整模型回代验证结果。

## 证据与质量状态

- 原始 PDF：48 页扫描件。
- OCR 文本：逐页完成，空页数见 `translation_notes.md`。
- Evidence：48 条，均保留页码或可追溯文本。
- 自动 IR 证据覆盖率：25.0%；语义后端超时后采用确定性回退，模型含义由本解读人工校订。
- 结论使用规则：普通方法描述可直接用于复习；精确参数、公式和小数结果在论文写作中引用前必须打开源 PDF 复核。
